import 'package:flutter/material.dart';

import 'index.dart';

class PlanningListPage extends StatelessWidget {
  const PlanningListPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const PlanningListView();
  }
}
